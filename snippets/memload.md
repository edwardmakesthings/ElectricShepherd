---
description: Pull relevant prior context out of MemPalace before proceeding
---

[Memory Recall] Before continuing, retrieve relevant prior context from MemPalace so you don't re-derive settled work, miss prior implementation, or contradict earlier decisions:

1. The three labeled blocks (project-state, active-conventions, user-preferences) are already loaded — re-read them and treat them as current ground truth.
2. Search the MemPalace diary and drawers for anything relevant to THIS task — prior decisions, prior IMPLEMENTATION work (what's already built), findings, and attempts. Use the search tool; don't assume you don't know something. Search first.
3. If this is a hard problem, check the apprenticeship room for a worked example of a similar problem before attempting it from scratch, and follow it if found.

Summarize in 2-3 sentences what's relevant: decisions to respect, work already done that you should build on rather than redo, or a worked example to follow. If nothing relevant turned up, say so and continue.