import TurnGuard from "./turn-guard.ts"

/**
 * Canonical plugin entrypoint.
 *
 * `turn-guard.ts` remains as a compatibility module while imports migrate.
 */
export const SessionPolicyPlugin = TurnGuard

export default SessionPolicyPlugin
